# air-cnc
prototype using JS6, Node, ReactJS, MongoDB (mongoose)

To test the application, tip:
If you have Yarn:
- yarn dev (background)
- yarn start (frontend)

Or you can use npm
npm run dev
npm start
